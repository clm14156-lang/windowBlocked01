# DEV_LOG.md

> **用途：让 AI / Codex 在最短时间内理解项目当前状态、最近进度、边界和下一步。**
>
> 本文件不是代码变更流水账，而是“当前状态摘要 + 重要历史记录”。  
> 每次进行有意义的功能修改、Bug 修复或设计决策后，AI 必须按下面规则维护本文件。

---

## 0. AI 必读与记录规则

### 0.1 每次开始任务前怎么读

AI / Codex 每次接手项目时，按以下顺序读取：

1. **先读「1. 当前项目状态」**：确认项目现在处于什么阶段。
2. **再读「2. 模块状态快照」**：确认目标模块目前做到哪里、数据是真是假。
3. **再读「3. 最近重要变更」**：了解最近刚改过什么，避免重复修改或破坏现有行为。
4. 只有需要追溯具体实现、Bug 原因或历史决策时，才继续搜索下面的「历史开发日志」。
5. UI 相关任务还必须同时读取 `UI_TYPOGRAPHY_STANDARD.md`，字体规范优先于单张参考图。

> **不要仅凭某个页面“看起来已经完成”就判断功能已经完成。**
> 必须查看该模块的“数据 / 后端状态”，区分 UI、内存交互、真实业务和持久化能力。

---

### 0.2 每次修改后怎么记录

只有以下情况需要写入日志：

- 完成一个功能闭环
- 修复一个真实 Bug
- 做出重要交互 / 架构 / 数据决策
- 完成一轮有明确结果的 UI 重构
- 项目阶段、模块状态、数据状态发生变化

以下情况**不要单独创建日志**：

- 单独改一个字体颜色
- 单独改 1～2px 间距
- 单独改一个圆角
- 同一轮 UI 调整中的连续微调
- 没有改变最终状态的反复尝试

这些细小修改应该合并到同一条功能 / UI 优化记录中。

---

### 0.3 每次记录必须同时维护 3 个地方

完成一次有意义的修改后：

1. **更新「1. 当前项目状态」**
   - 只有状态真的变化才修改。
   - 不要重复堆历史内容。

2. **更新「3. 最近重要变更」**
   - 只保留最近 **5 条**。
   - 新记录放最上面，最旧的一条移除。

3. **在文件底部「历史开发日志」追加 1 条记录**
   - 历史日志只追加，不删除已有历史。

---

### 0.4 历史日志统一格式

以后新日志统一使用：

```md
## YYYY-MM-DD 模块 / 主题

### 完成
- 这次最终完成了什么。

### 当前状态
- UI：完成 / 未完成
- 交互：完成 / 部分完成 / 未完成
- 数据：Mock / 内存 / 本地持久化 / 真实数据
- 后端：未接入 / 已接入
- 明确写出仍未实现的边界。

### 验证
- 构建、测试、截图、真实鼠标回归等最终验证结果。
- 小型视觉修改可只写“验证通过”。

### 下一步
- 只写最自然的下一步。
- 如果没有明确下一步，写“无”。
```

### 0.5 记录原则

- **记录最终结果，不记录详细操作过程。**
- **记录当前事实，不记录猜测。**
- **同一功能的一系列小修改合并成一条。**
- **UI 完成 ≠ 业务完成，必须明确数据和后端状态。**
- 不重复大段架构说明；只有架构发生变化时才记录。
- 测试数量只有在本轮真正执行完整测试时才写。
- 如果本次修改没有改变项目状态，不要为了“每次都记”而制造无意义日志。
- `当前项目状态` 是 AI 判断项目现状的第一信息源；历史日志只用于追溯。

---

## 1. 当前项目状态

**最后更新：2026-08-22**

### 当前阶段

**阶段二：前端 UI 与基础交互持续完善。**

目前主要页面、弹窗和核心前端流程已经基本搭建完成，正在进行：

- 全局 UI 一致性整理
- Typography / 字体规范统一
- 组件视觉与交互状态统一
- 已有页面的细节校准和 Bug 修复

### 当前技术结构

- `.NET 8`
- WPF
- MVVM
- 项目结构：
  - Desktop
  - Agent
  - Service
  - Core
  - Infrastructure
  - Contracts
  - Tests
- 主窗口主要按 **800 × 710 DIP** 进行视觉验收。

### 当前数据 / 后端状态

- 大部分页面数据仍然是 **Mock / 当前运行期内存数据**。
- 登录、会员、目标、任务、统计、自动屏蔽规则等大部分业务尚未持久化。
- 数据库：**未正式接入**
- 服务器 / API：**未正式接入**
- IPC：**未正式接入核心业务**
- Service / Agent：已有运行骨架，但核心业务尚未完整接入。
- 当前已经存在少量真实本地能力，例如：
  - 专注计时与窗口交互
  - exe 图标提取
  - favicon 异步获取
  - 悬浮窗吸附 / 折叠 / 置顶等窗口行为
- **真实系统级网站 / 应用屏蔽能力仍不能仅根据当前 UI 判断为已完成。**

### 当前 UI 规范

UI 相关修改必须优先遵守：

- `UI_TYPOGRAPHY_STANDARD.md`

当前正在解决的历史问题：

> 早期页面主要按照各自参考图独立还原，导致不同页面字号、字重和文字颜色缺少统一标准。

之后所有页面应优先服从全局 Typography，再进行参考图视觉校准。

### 当前重点

1. 统一全项目字体层级、字号、字重和文字颜色。
2. 统一按钮、输入框、列表、标签等组件的视觉和交互状态。
3. 继续修复阶段二现有 UI / 前端交互问题。
4. 前端 UI 稳定后，再按开发计划推进真实业务、持久化与后端能力。

### 当前已知边界 / 注意事项

- 不要把 Mock 数据误认为正式业务数据。
- 不要因为页面 UI 已存在就假设数据库、服务器或后台功能已经接入。
- 视觉优化任务默认不得改变现有业务逻辑，除非需求明确要求。
- 专注目标弹窗曾遇到无边框 WPF 截图捕获限制，自动截图结果可能不可靠。
- 后续 UI 修改应避免重新产生页面间字体标准不一致的问题。

---

## 2. 模块状态快照

| 模块 | UI | 前端交互 | 数据状态 | 当前说明 |
|---|---|---|---|---|
| 首页 | 已完成并持续优化 | 已完成主要交互 | 内存 / Mock | 包含专注时间、目标、当前屏蔽等 |
| 登录 / 注册 / 找回密码 | 已完成 | 已完成前端流程 | 内存假账号 | 未接真实账号服务器 |
| 个人信息 / 账号与同步 | 已完成 | 已完成基础交互 | Mock | 未接真实同步与设备管理 |
| VIP / 会员中心 | 已完成 | 已完成前端选择流程 | Mock | 未接支付与真实会员系统 |
| 专注流程 | 已完成 | 5 秒准备、计时、结束、完成流程已实现 | 内存 | 未正式持久化专注记录 |
| 目标 / ToDo | 已完成并持续优化 | 创建、选择、编辑、删除、完成等已实现 | 内存 | 尚未正式持久化 |
| 自动屏蔽规则 | 已完成主要 UI / 交互 | 创建、校验、预览、到点触发等已有前端逻辑 | 内存 | 未正式持久化 |
| 访问控制 | 已完成主要 UI | 网站 / 应用列表与开关交互已实现 | 内存 | 真实系统级屏蔽能力尚未完整接入 |
| favicon / 程序图标 | 已完成 | 异步加载 | 部分真实本地能力 | favicon 获取、exe 图标提取已有实现 |
| 悬浮窗 | 已完成主要状态 | 吸附、折叠、Hover 展开、置顶等已实现 | 运行期状态 | 主要属于真实窗口行为 |
| 统计 | 已完成主要页面 | 趋势、日历、目标联动已实现 | 统一 Mock 专注记录 | 未接真实持久化统计数据 |
| 设置 | 已完成 | 开关与入口前端交互已实现 | 内存 | 多数系统设置功能尚未真实接入 |
| 主题 | 已完成主要 UI | 主题选择基础交互已实现 | 当前运行期状态 | 正在统一主题按钮文字模板和字体标准 |

---

## 3. 最近重要变更

> 只保留最近 5 条。每次新增一条时删除最旧的一条。

1. **2026-08-22｜统计趋势图 Y 轴坐标映射修复**
   - 统一 Y 轴刻度、折线锚点和面积基线的 PlotArea 映射，确保 0 值精确落在 0h / X 轴基线上。

2. **2026-08-22｜主题按钮文字基础模板**
   - 新增全局 `ThemedButtonTextContentTemplate` 与 `ThemedTextButtonBaseStyle`，避免隐式 TextBlock 样式覆盖按钮文字属性。

3. **2026-08-22｜专注目标选择弹窗视觉统一**
   - 统一标题、正文、标签、按钮和交互状态，并移除操作按钮的渐变与明显阴影。

4. **2026-08-22｜首页已选专注目标居中**
   - 使用左右对称三列结构，使目标名称相对整个胶囊真正居中。

5. **2026-08-21｜首页当前屏蔽卡片视觉优化**
   - 降低卡片视觉重量，统一标题、副标题、图标和边框表现。

---

## 4. AI 快速判断原则

接手一个具体模块时，先问自己四个问题：

```text
1. 这个模块 UI 做完了吗？
2. 交互做完了吗？
3. 当前数据是真的还是 Mock / 内存？
4. 下一步到底是继续前端，还是已经可以接真实业务？
```

如果这四个问题不能从上面的状态摘要中确定，再去搜索历史日志。

---

# 历史开发日志

> 以下内容用于追溯具体实现、Bug 原因和历史决策。  
> 默认按时间顺序追加。AI 日常接手项目时不需要从头全文阅读。

## 2026-08-15 模块 1：解决方案与项目骨架

### 完成了什么

- 创建可还原、可编译的 .NET 8 解决方案及 7 个项目骨架。

### 修改了什么

- 添加 Desktop、Agent、Service、Core、Infrastructure、Contracts 和 Tests 项目。
- 添加统一编译配置、SDK 版本配置和 Git 忽略规则。

## 2026-08-15 模块 2：项目依赖与模块边界

### 完成了什么

- 建立并验证符合架构约束的项目引用关系。

### 修改了什么

- 配置 Desktop、Agent、Service、Infrastructure 和 Tests 的项目引用。

## 2026-08-15 模块 3：Desktop 最小启动外壳

### 完成了什么

- 建立可启动、可显示和可正常关闭的 WPF MVVM 主窗口外壳。

### 修改了什么

- 添加主窗口、主窗口 ViewModel，并配置 Desktop 应用启动与关闭流程。
- 将主窗口初始及最小尺寸设置为 800 × 710。

## 2026-08-15 模块 4：基础资源体系

### 完成了什么

- 建立并验证 Desktop 的基础资源加载体系。

### 修改了什么

- 添加语义颜色、字体尺寸、界面文字和基础控件样式资源字典。
- 配置应用级资源合并，并让主窗口通过动态资源显示基础样式与文字。

## 2026-08-15 模块 5：基础导航框架

### 完成了什么

- 建立并验证首页、屏蔽、统计、设置和账号入口的基础导航切换。

### 修改了什么

- 添加导航 ViewModel、导航命令、选中状态和线性占位图标。
- 添加 80 DIP 左侧导航布局、占位内容区及导航切换单元测试。

## 2026-08-15 模块 6：Service 与 Agent 最小运行骨架

### 完成了什么

- 建立并验证 Service 与 Agent 的启动、持续运行和关闭生命周期。

### 修改了什么

- 为 Service 添加 Windows Service 兼容的 Generic Host 和空后台 Worker。
- 为 Agent 添加用户会话 Generic Host 和空后台 Worker。

## 2026-08-15 模块 7：阶段一整体运行验收

### 完成了什么

- 完成解决方案构建、测试、架构边界、Desktop 交互和多进程联合运行验收。
- 确认阶段一工程基础达到完成标准。

### 修改了什么

- 未修改功能代码，仅添加阶段一整体验收记录。

## 2026-08-15 阶段二模块 1：首页 UI 与基础交互

### 完成了什么

- 按首页参考图完成 800 × 710 DIP 首页静态界面，并在 150% DPI 环境下完成视觉验收。
- 完成导航选中、时长 Hover/选中、开始专注 Hover/Pressed、窗口按钮 Hover，以及目标和屏蔽规则入口的点击反馈。

### 修改了什么

- 新增首页 View、假数据 ViewModel、首页文字资源和通用控件状态样式。
- 主窗口改为自绘标题栏与首页布局；未添加弹窗、真实业务、数据库、IPC 或后台能力。
- 按整图比例完成首页位置与尺寸校准，并通过 800 × 710 DIP 叠图检查主要组件轮廓。

## 2026-08-16 阶段二模块 2：登录与注册弹窗

### 完成了什么

- 完成当前首页上的登录模态弹窗、注册态切换和半透明遮罩关闭交互。
- 登录态为 360 × 360，注册态为 360 × 450，使用假字段和占位图标。

### 修改了什么

- 账号入口改为打开认证弹窗，不再切换导航页面。
- 新增认证弹窗 View、认证状态 ViewModel、交互样式和对应资源；未接入真实登录、注册、验证码、服务器或数据存储。

## 2026-08-16 登录切换交互修复

- 修复认证卡片 `PreviewMouseDown` 提前标记事件导致登录 / 注册按钮无法触发 Click 的问题。
- 改为普通鼠标冒泡处理，保留卡片空白区域与遮罩关闭行为；已通过真实鼠标回归验证。

## 2026-08-16 登录 / 注册弹窗叠图校准

- 按 360 × 360 与 360 × 450 同尺寸叠图校准标题、页签、输入框、按钮、提示与协议区域的字号、尺寸、间距和对齐。
- 保持现有颜色、交互和业务范围不变；Release 构建、3 项测试及登录 / 注册切换与蒙版关闭回归均通过。

## 2026-08-16 注册子页面视觉校准

- 按最新注册参考图将注册弹窗高度校准为 465 DIP，并重新校准副标题、字段字号、图标、输入框高度与表单间距。
- 保持登录页、颜色、交互和业务逻辑不变；Release 构建、3 项测试及注册态叠图复查通过。

## 2026-08-16 阶段二模块 3：自定义专注时间弹窗

- 按参考图完成固定 300 × 360 DIP 自定义时间弹窗，包含标题、90 分钟输入区、8 个常用时间占位按钮及取消 / 添加操作区。
- 接入首页自定义入口、取消关闭和添加后回填选中时长的假数据交互；未实现常用时间选择、增删或真实业务能力。
- 完成同尺寸截图叠图校准、真实鼠标交互回归、Release 构建及 4 项测试。

## 2026-08-17 阶段二模块 4：皮肤主题浮层

- 按参考图完成固定 420 × 450 DIP 皮肤主题浮层，包含纯色、渐变和个性主题三组假数据预览。
- 接入衣服按钮打开、关闭按钮、浮层外关闭、Hover 与单选预览交互；未接入正式主题资源管理或后台能力。
- 建立 `Assets/Themes/分类/主题名/` 独立占位目录，并完成多轮同尺寸叠图和真实鼠标回归。

## 2026-08-17 阶段二模块 5：设置页面

- 按参考图完成设置页基础布局，包含 6 项常规设置和“导出记录”“关于应用”两个入口。
- 接入内存开关、Hover、Pressed 与入口点击状态；未实现开机启动、通知、导出等真实业务能力。
- 完成页面往返、真实鼠标交互、截图校准、Release 构建及 8 项测试。

## 2026-08-17 阶段二模块 6：自动屏蔽规则 UI

- 完成自动屏蔽规则展开区、空状态、纵向规则列表、编辑占位和单条删除交互。
- 完成 270 × 360 / 270 × 440 新建规则弹窗、每天 / 自定义切换、星期多选及时间输入与双滑块同步。
- 全部状态仅使用内存假数据；完成截图叠图、真实鼠标交互、Release 构建及 11 项测试。

## 2026-08-17 阶段二模块 7：屏蔽页面 UI

- 完成网站 / 应用程序 Tab、占位列表、开关、单条删除及网站新增内存交互；网站编辑仅保留占位 UI。
- 完成 345 × 295 添加网站弹窗和 500 × 420 添加程序弹窗；未读取程序记录或调用系统选择器。
- 完成同尺寸截图叠图、真实鼠标交互、Release 构建及 16 项测试；未接入真实屏蔽、数据库、IPC、API 或后台能力。

## 2026-08-17 阶段二模块 8：专注前端流程

- 完成首页开始后的 5 秒准备倒计时、取消、专注倒计时、结束确认暂停 / 继续、提前或自然完成及返回首页流程。
- 将 5 秒准备倒计时保留在完整首页内，仅切换中央按钮状态；环形进度改为基于实际经过时间的连续毫秒级动画。
- 新增原生 WPF 环形进度、330 × 320 结束确认弹窗和专注完成页；时长、今日累计与完成时间仅使用内存临时状态。
- 按最新专注页参考图比例校准 800 × 710 页面，移除“剩余时间”，并通过半透明叠图调整圆环、数字、状态行和结束按钮。
- 完成 800 × 710 离屏截图校准、Release 构建及 24 项测试；未接入真实记录、数据库、IPC、API、服务器或后台能力。

## 2026-08-17 阶段二模块 9：本次专注目标

- 新增 320 × 435 本次专注目标弹窗，支持最近目标切换、超过 3 项时翻页、新建目标及按目标切换待执行任务。
- 新增任务输入自动聚焦与 Enter 创建、任务编辑和删除菜单；全部状态仅保存在内存中。
- 保持现有专注页面布局和专注流程不变，完成离屏截图校准、Release 构建及 29 项测试。

## 2026-08-17 登录输入与假数据登录

- 将登录和注册弹窗中的静态输入占位区域替换为可实际输入的文本框与密码框。
- 接入账号 `123`、密码 `123` 的内存假登录；错误凭据显示提示，登录状态不持久化且不连接服务器。
- 登录成功后关闭弹窗，左下角切换为默认头像并隐藏“未登录”与加号徽标；再次点击头像不再打开登录弹窗。
- Release 构建通过且无警告，34 项测试全部通过；未接入真实账号、数据库、IPC、API 或后台能力。

## 2026-08-17 个人信息浮动窗口

- 新增固定 190 × 250 DIP 的个人信息浮层，并定位在首页左下角头像旁。
- 展示默认头像、已登录状态、假数据邮箱、账号与同步、开通 VIP 和退出登录入口；文字字号保持在 11～15 DIP，图标独立使用正常字号。
- 接入头像切换浮层、点击外部关闭及内存退出登录；账号与同步和开通 VIP 仅保留 UI。
- 退出后恢复未登录头像状态和登录弹窗入口；Release 构建通过且无警告，35 项测试全部通过。

## 2026-08-17 开通 VIP 弹窗

- 新增固定 440 × 550 DIP 的居中 VIP 模态弹窗，包含会员权益卡片、月会员、年会员和永久会员三种假方案。
- 接入全窗口轻度遮罩、遮罩外关闭、右上角关闭、稍后再说关闭及三种方案单选切换。
- “立即升级”仅显示内存点击反馈，不接入支付、会员状态、服务器或其他真实业务能力。
- 从个人信息浮层打开时自动收起浮层，同一时间仅保留一个 VIP 弹窗状态；Release 构建通过且无警告，39 项测试全部通过。

## 2026-08-17 账号与同步窗口

- 新增固定 650 × 400 DIP 的居中账号与同步窗口，左侧提供云端同步、登录设备和账号安全导航。
- 右侧加入云端同步、同步内容、登录设备、账号安全和危险操作完整内容，超出首屏部分通过纵向滚动条浏览。
- 接入左侧导航定位、右上角关闭和内存假数据展示；未接入真实同步、设备管理、密码修改、注销账号或服务器能力。
- 完成 650 × 400 实际渲染截图与参考图叠图校正，保留 QA 截图；Release 构建通过且无警告，45 项测试全部通过。

## 2026-08-17 三种假账号与会员状态

- 新增三组固定测试账号：`123 / 123` 普通用户、`456 / 456` 年度会员、`789 / 789` 永久会员；仅保存在内存中，不连接服务器。
- 普通用户继续显示原“开通 VIP”浮窗入口；年度和永久会员显示 VIP 边框/标识，并将入口替换为“会员中心”，隐藏升级标签。
- 新增年度 / 永久共用会员中心组件：年度显示假到期日、剩余 128 天和续费按钮，永久仅显示“永久有效”，不显示到期、天数和续费区域。
- 完成普通浮窗、VIP 浮窗、年度会员中心和永久会员中心 QA 渲染截图及叠图复查；Release 构建通过且无警告，56 项测试全部通过。

## 2026-08-17 阶段二模块 10：带目标的专注页面

- 新增带目标专注模式：开始前明确选择目标时显示目标信息、未完成任务和已完成任务分组；未选择目标时保持原默认圆环专注页面。
- 在不改变准备倒计时、专注倒计时、结束确认和完成流程的前提下，新增内存任务的添加、Enter 创建、完成切换、重命名、删除、菜单关闭和已完成折叠交互。
- 新增目标模式分流与任务分组测试；Release 构建通过且无警告，59 项测试通过。

## 2026-08-17 启动异常修复

- 删除 `Strings.xaml` 中重复的 `FocusRemainingTime` 资源键，修复 WPF 启动阶段因资源字典重复键导致的静默退出。
- 将带目标专注页面进度条的 `RemainingProgress` 绑定改为 `OneWay`，修复只读属性被默认双向绑定导致的启动异常。
- 将已完成任务数量到可见性的绑定改为 `OneWay`，修复第二处启动阶段只读 `Count` 属性绑定异常。
- 将已完成任务数量文本的 `Run.Text` 绑定改为 `OneWay`，修复同一只读 `Count` 属性的显示绑定异常。

## 2026-08-17 目标专注页点击崩溃修复

- 移除任务行 `MouseEnter` 中对 `Border.Visibility` 的对象动画，修复点击任务时触发 `AnimationException` 导致窗口秒退的问题。

## 2026-08-17 目标任务菜单重复显示修复

- 为专注页面任务菜单增加独立的 `IsFocusMenuOpen` 状态，避免与目标选择弹窗的旧 `Popup` 菜单共享状态后同时显示两个菜单。

## 2026-08-17 ToDo 内部滚动修复

- 移除目标专注页外层滚动容器，为 ToDo 卡片设置固定任务区域，并将任务列表、添加任务和已完成任务分组的滚动限制在卡片内部。

## 2026-08-17 带目标专注页视觉校准

- 按目标设计稿调整目标专注页两张卡片的位置、宽度、高度、内边距、任务行高、字号、分割线和结束按钮尺寸；未修改计时与任务交互逻辑。

## 2026-08-18 专注完成页目标分支

- 有目标的专注完成后新增任务完成统计页，显示本次实际完成任务、今日累计、完成时间及原有操作按钮；无目标时继续使用默认完成页。
- 新增会话级任务完成快照，完成页分支只依据开始专注时是否选择目标；目标存在但完成 0 个任务时仍显示目标完成页。

## 2026-08-18 自动屏蔽规则预览与校验

- 设置页新增自动屏蔽规则的重复/覆盖校验：完全重复和被覆盖区间拒绝，部分重叠允许独立保存。
- 首页当前屏蔽卡片显示下一条启用规则的最近执行时间，并提供持续时长提示；规则创建和删除后立即刷新内存预览。

## 2026-08-18 首页自动屏蔽提示简化

- 首页当前屏蔽卡片改为只在标题行显示“当前屏蔽 · 时间”，移除下方详情文字；详细开始时间和持续时长保留在时间 Hover 提示中。

## 2026-08-18 自动屏蔽总开关同步修复

- 设置页自动屏蔽总开关变化立即通知首页；总开关关闭时清除最近规则预览，重新开启后恢复计算。

## 2026-08-18 新建自动屏蔽规则校验提示位置调整

- 将重复/覆盖规则的校验提示移入“新建规则”居中弹窗，并在提示时保持弹窗打开。
- 移除设置页“屏蔽规则”区域下方的校验提示展示，保留原有校验逻辑。

## 2026-08-18 自动屏蔽到点触发专注

- 主窗口按秒评估启用中的自动屏蔽规则，到达有效时间段时复用首页现有开始专注核心流程。
- 增加连续时间段去重，避免轮询、重叠规则或重复规则导致重复启动；应用启动时处于有效时间段也会立即识别。

## 2026-08-18 自动屏蔽当天完成状态修复

- 首页自动屏蔽预览改为只计算当天尚未执行的规则，不再在当天结束后跳到次日同一时间。
- 记录规则当天的完成状态；规则时间段结束后立即刷新首页，次日按日期重新生效。

## 2026-08-18 添加网站与 favicon 异步加载

- 添加网址保存后立即创建内存网站项并显示默认图标，不等待网络请求。
- 新增 `IFaviconService` / `FaviconService`，按域名进行本次运行内存缓存，异步获取 favicon，失败或超时保持默认图标。

## 2026-08-18 首页当前屏蔽与已屏蔽内容弹窗

- 首页当前屏蔽区域同步访问控制中已开启的网站和应用，最多展示 3 个图标及额外数量，并实时更新启用数量。
- 新增固定 550×360 的“已屏蔽内容”弹窗，支持全部/网站/软件筛选；垃圾桶仅关闭原规则开关，三处状态共用同一份内存数据。

## 2026-08-18 已屏蔽内容取消屏蔽按钮修复

- 将弹窗行内垃圾桶按钮改为显式调用弹窗统一的取消屏蔽命令，确保关闭原规则开关后立即刷新弹窗和首页，原始规则仍保留。

## 2026-08-18 已屏蔽内容交互与滚动修复

- 取消屏蔽时先移除弹窗当前行，再关闭原始规则开关，避免刷新重建导致点击无视觉反馈。
- 将全部/网站/软件列表改为单一受约束高度的滚动容器，修复滚动条无法操作的问题。

## 2026-08-18 首页屏蔽图标真实 favicon 优先

- 首页预览仅对直接展示的 3 个内容按真实 favicon 优先排序，默认图标用于补位。
- 保持访问控制和“已屏蔽内容”弹窗的原始顺序，`+N` 继续统计所有未直接展示的内容。

## 2026-08-18 强制模式专注页状态

- 开始专注时快照强制模式开关；开启时隐藏提前结束按钮，显示不可点击的盾牌提示。
- 强制模式会拦截提前结束请求，但不暂停或修改倒计时；关闭时保留原有结束专注流程。

## 2026-08-18 专注悬浮窗边缘吸附与折叠

- 用户需求：悬浮窗支持多显示器边缘吸附、任务栏边缘排除、横向/竖向折叠、Hover 展开和离开后自动收回。
- 新增 WorkArea 与任务栏边缘识别、吸附状态机、折叠尺寸和屏幕内侧展开位置计算。
- 保持专注倒计时、进度和任务状态复用同一份内存状态，未接入真实业务或后台能力。

## 2026-08-18 悬浮窗折叠尺寸修复

- 将普通 `300×220` 悬浮窗与横向/竖向折叠窗拆分为独立 Window 文件。
- 修复普通窗口固定最小/最大尺寸导致折叠窗实际尺寸异常的问题。

## 2026-08-18 悬浮窗四边吸附修复

- 修复 Win32 物理屏幕坐标与 WPF DIP 坐标换算不一致导致左右边无法触发的问题。
- 按最新需求允许上、下、左、右四边吸附，并同时识别显示器边缘与任务栏形成的 WorkArea 边缘。

## 2026-08-18 专注悬浮窗单窗口结构重构

- 用户需求：折叠态不再创建独立 Window，为后续边缘推拉动画建立单窗口结构和统一状态机。
- 保留一个 `FocusFloatingWindow` 管理位置、拖拽和吸附；完整、横向折叠、竖向折叠内容拆为三个 UserControl，共享原倒计时和任务 ViewModel。
- 新增 `Floating / Folded* / Expanding / ExpandedFromFold / Collapsing` 状态基础，Hover 展开与离开收回均在同一窗口内切换。

## 2026-08-18 悬浮窗跨边缘吸附判定修复

- 修复窗口拖过 Windows 左、右、下边缘后，因远端边缘距离变大而无法吸附的问题。
- 吸附判定改为检测窗口矩形与屏幕边缘是否相交；相交时距离按 0 处理，保留原有四边折叠尺寸和状态逻辑。

## 2026-08-18 侧边折叠态样式调整

- 用户需求：侧边竖向折叠态圆角调整为 `15`，剩余时间字体调整为 `22px`。
- 上下横向折叠态剩余时间字体调整为 `22px`，圆角调整为 `12px`。

## 2026-08-19 阶段二模块 11：统计页面总览

- 新增统计总览页面，包含今日专注概览、近 7 天 / 近 30 天投入趋势与周期汇总信息。
- 趋势图使用内存 Mock 数据；切换范围会更新日期、时长、曲线、总计、日均及环比数据。
- 数据点支持 Hover 高亮和日期、时长、专注次数 Tooltip；30 天模式保留全部数据点与曲线，按间隔显示横轴标签以避免横向滚动。
- 未接入数据库、IPC、API、服务器或任何真实统计数据源；Release 构建及 87 项测试通过。
- 按总览设计稿完成多轮 800 × 710 截图与半透明叠图校正：收紧顶部节奏和卡片高度，重绘平滑趋势曲线、面积、坐标轴与统计栏，并补齐本月专注目标模块。
- 修复趋势图坐标错位：曲线、圆点、数值标签复用同一组节点坐标，日期与星期改为共用节点 X 坐标和固定 X 轴 Y 坐标。
- 修复趋势图 Y 轴刻度：按当前范围最高单日时长向上取 4 小时倍数，刻度和曲线共用比例映射。
- 完善近 7 天 / 近 30 天趋势切换：30 天保留全部曲线数据，仅默认显示 7 个均匀代表节点，隐藏节点支持 Hover 临时显示并同步切换本月文案。
- 修复趋势 Hover 提示：隐藏日期不再出现在 X 轴下方，改为数据点上方 Tooltip，并增加临时高亮和淡色垂直辅助线。
- 将 Hover Tooltip 移出 TrendChart Canvas，改为趋势卡片外层 Popup，并按卡片边界自动修正定位，避免被图表绘制层裁切。

## 2026-08-19 统计目标页交互修复

- 推进记录改为显示全部跨月历史并倒序排列，展开箭头仅在行 Hover 时显示。
- 投入趋势 Tooltip 固定在柱子上方；X 轴仅显示当月首尾日期，并随月份同步刷新数据与日期。
- 统一目标推进记录与趋势图 Mock 数据源，记录增删改会同步刷新历史列表和当月柱图。
- 月份下拉框改为按当前目标历史记录动态生成并倒序排列，随记录增删改同步更新。
- 为每个目标建立独立 Mock 推进记录，切换目标时同步刷新汇总、月份、趋势和历史。

## 2026-08-19 统计日历与目标数据同步

- 日历与目标页统一使用同一份 Mock 专注记录，日期详情、目标趋势和月度投入分布保持同步。

## 2026-08-19 目标页新增目标

- “+”按钮支持行内创建并自动聚焦命名；Enter 或失焦保存，新目标自动选中且初始统计为空。

## 2026-08-19 日历测试数据

- 为 2026 年 2 月 20 日追加 5 条“读书”专注 Mock 记录。

## 2026-08-19 目标趋势与推进记录联动

- 推进记录滚动按主要可见月份同步趋势；点击趋势柱子自动展开并定位对应日期记录，程序化滚动期间抑制循环切月。

## 2026-08-19 总览本月专注目标

- 新增本月专注目标设置、编辑、删除及进度展示，完成时长直接跟随统一 Mock 专注记录刷新。
- 修复只读目标进度文本绑定导致应用启动异常的问题。
- 设置/编辑浮窗改为跟随触发按钮并居中显示在其上方，再次点击按钮可关闭。

## 2026-08-19 最近运行程序列表

- “添加应用到控制列表”弹窗将“最近三天运行的程序”改为“最近运行”，并展示不受三天限制的内存 Mock 历史记录。
- 最近程序按 ExePath 去重、按 LastRunTime 倒序，重复运行只更新时间；过滤典型系统进程并支持名称、进程名和路径搜索。
- 支持选择最近程序并加入内存控制列表，保留“选择程序”占位入口；未读取本机进程、文件系统或接入持久化、IPC、API 和后台能力。

## 2026-08-19 最近运行程序图标

- 最近运行列表保留默认占位图标，并在后台通过 `Icon.ExtractAssociatedIcon` 按 exe 路径提取真实图标。
- 图标转换为冻结的 WPF `BitmapSource`，以 exe 路径做本次运行内存缓存；完成后仅通过 Dispatcher 更新仍在打开弹窗中的列表项，失败时保留占位图标。

## 2026-08-19 最近运行程序路径校正

- 修正 Chrome 的用户目录安装路径；不存在的 Mock exe 路径由图标服务安全回退为默认占位图标。

## 2026-08-19 最近运行程序占位图标回退

- 恢复完整的最近运行 Mock 列表；存在的 Chrome、Edge 路径优先显示真实图标，未安装程序继续显示默认占位图标而不从列表中移除。

## 2026-08-19 专注目标最近使用交互修复

- 移除最近目标选中后的独立“×”按钮；再次点击当前目标直接取消选中，点击其他目标切换选中项。
- 最近目标改为有限滑动窗口，左右箭头按真实边界显示，滑到首尾后停止且不再循环。
- 左右翻页按钮拆分到最近使用行的独立固定列，左箭头始终位于列表左侧，右箭头始终位于列表右侧，隐藏时不会互相替换位置。

## 2026-08-19 待执行任务菜单 Hover 优化

- 任务行默认隐藏右侧“...”入口，Hover 当前任务时显示；其他任务保持隐藏。
- 任务菜单打开后保留“...”入口，直到菜单关闭；按钮隐藏使用 Hidden 保留原占位，避免任务文字跳动。
- 修复任务菜单按钮将 Style 错误赋给 Visibility 导致目标弹窗首次生成任务模板时运行时退出的问题。

## 2026-08-19 首页专注目标文字裁剪修复

- 首页“本次专注目标（可选）”按钮小幅增加宽度并收紧左右内边距，确保右括号完整显示；字号、居中和整体布局保持不变。

## 2026-08-20 专注悬浮窗置顶与非抢焦点

- 悬浮窗统一启用置顶层级，悬浮、折叠、Hover 展开和重新折叠过程中持续保持在普通应用窗口之上。
- 禁止悬浮窗显示和复用时主动激活，避免置顶行为抢夺用户当前应用的输入焦点。
- 恢复正常主窗口时仍取消悬浮窗置顶状态并使用原有普通窗口逻辑；未接入后台或真实业务能力。

## 2026-08-20 修复开启悬浮窗口设置无效

- 专注中点击最小化前即时读取“开启悬浮窗口”内存设置。
- 开启时进入悬浮窗并继续支持折叠、吸附和置顶；关闭时直接按普通 Windows 窗口最小化到任务栏。

## 2026-08-20 修复目标任务行 Hover 命中区域

- 为创建目标弹窗的待执行任务行最外层容器补充透明背景，使整行空白区域也参与鼠标命中测试。
- 保持现有任务菜单、编辑和删除交互不变。

## 2026-08-20 精简本次专注目标已选文案

- 未选择目标时继续显示“本次专注目标（可选）”，选择后仅显示实际目标名称。
- 保持按钮原有尺寸、圆角、内边距和图标位置，长目标单行省略并可通过 Tooltip 查看完整内容。

## 2026-08-20 本次专注目标标签化样式

- 移除目标控件右侧箭头；未选择状态保持原宽度，已选择状态按目标文字自适应为紧凑胶囊。
- 为目标名称保留最大宽度和单行省略，继续支持点击重新选择及 Tooltip 查看完整名称。

## 2026-08-20 已选择目标胶囊视觉居中

- 为已选择状态增加右侧补偿留白，平衡左侧铅笔图标的视觉重量，使短目标文字在自适应胶囊中更自然地居中。
- 未选择状态、垂直对齐、长文本省略和 Tooltip 行为保持不变。

## 2026-08-20 目标弹窗底部操作状态

- 无目标时底部仅显示“创建新目标”；选中目标后切换为固定高度的“取消 / 确认”操作区。
- 取消会清空当前选择并恢复创建入口，确认保留选择并关闭弹窗；右上角关闭会恢复打开弹窗前的选择。

## 2026-08-20 重做常用专注时间管理

- 固定保留首页“自定义”入口；新增时长加入独立常用时间列表，不再覆盖自定义按钮。
- 首页仅显示已选常用时间与固定自定义入口，最多显示 4 个常用时间并始终保持水平居中。
- 添加时间弹窗支持常用时间选中/取消显示、编辑模式、删除、完成和弹窗外退出；删除同步移除首页显示。

## 2026-08-20 添加专注时间弹窗输入与布局修复

- 常用时间胶囊固定为每行 3 个，统一宽度和间距。
- 点击“添加”后保留弹窗打开并立即刷新列表，输入重置为默认值以便继续添加。
- 输入限制为 1~999 分钟，仅接受最多 3 位数字，空值和非法值不会创建时间块。

## 2026-08-20 修复时间块显示与当前选择状态混用

- `IsSelected` 仅表示常用时间是否显示在首页，新增独立的 `IsCurrent` 表示当前专注时长。
- 首页点击时间块只切换当前专注时长，不再取消其他已显示时间块。
- 添加弹窗中的多个选中胶囊仅代表首页显示状态，新增、删除和当前专注选择互不覆盖。

## 2026-08-20 时间块隐藏分钟单位

- 首页和添加专注时间弹窗中的时间块仅显示数字，输入框右侧的“分钟”单位保持不变。
- 未修改时长数据、选择状态和专注逻辑。

## 2026-08-20 常用时间满额自动轮换

- 常用时间首页显示达到 4 个时，选择新的时间会自动移除最早显示项并追加新项。
- 点击已显示时间只切换当前专注时间，不再取消其首页显示；删除仍仅通过编辑模式执行。

## 2026-08-20 常用时间显示状态可切换

- 弹窗中点击已选常用时间会取消其首页显示但保留常用时间；再次点击可重新加入首页。
- 当前专注时间状态与首页显示状态继续独立维护。

## 2026-08-20 常用时间数量上限

- 常用时间最多允许 9 个；第 10 个及之后的新增请求会失败并保持输入值。
- 只有成功新增时输入框才重置为 0，弹窗始终保持打开。

## 2026-08-20 首页时间块组居中

- 将首页时间按钮区域的横向内容和内部按钮组统一设置为居中对齐。
- 时间块数量变化时整组自动重新居中，保留原有按钮尺寸、间距和交互逻辑。

## 2026-08-20 统计页面 UI 高还原

- 按设计稿调整统计页总览、日历和目标三个子页面的卡片尺寸、间距、字体层级与对齐，并通过 800×710 截图叠图校正。
- 日历页重点优化日期网格、选中日期、详情记录和目标投入分布；目标页优化目标列表、趋势图与推进记录样式。
- 保持顶部 Tab、左侧导航栏、窗口尺寸及现有数据和交互逻辑不变；Release 构建通过，120 项测试全部通过。

## 2026-08-20 统计目标页设计图 2 高还原重构

- 按设计图 2 将主导航缩窄为 72 DIP，并重新校准统计页顶部 Tab、目标页左右栏比例、卡片边界、留白和字体层级。
- 目标列表改为更紧凑的苹果风格卡片与细滚动条，保留选择、Hover、重命名、归档和新增等原有交互；展示数据仍为内存 Mock。
- 投入趋势补齐 0h 起始刻度、2h / 4h 动态间隔、24h 上限、7 个代表日期、水平虚线、圆角渐变柱和原有 Hover 提示。
- 推进记录按日期、时间范围、推进数、专注时长和箭头重新排版，保留点击联动、展开与滚动同步逻辑。
- 完成 800×710 三轮截图叠图校正并保留最终 QA 图；Release 构建通过，123 项测试全部通过，未接入数据库、IPC、API、服务器或后台能力。

## 2026-08-20 访问控制网站页设计图 1 高还原优化

- 仅调整访问控制页右侧网站内容区，校准标题说明、顶部 Tab、摘要卡片、列表边界、列宽、行高、字体层级、留白、圆角与轻量阴影；左侧侧边栏保持不变。
- 网站列表由紧凑表格优化为苹果设置式列表，保留默认网站图标与屏蔽开关逻辑；直接编辑/删除入口收束为“...”悬浮菜单，并继续绑定原有重命名与删除命令。
- 完成 800×710 截图叠图校正，验证菜单打开、外部点击关闭配置和命令绑定；Release 构建通过，123 项测试全部通过，未修改数据、页面跳转或业务交互逻辑。

## 2026-08-20 设置页设计图 1 高还原优化

- 仅调整设置页右侧内容区，将标题、分组标题、常规设置卡片和数据与应用卡片按设计图重新校准宽度、行高、间距、圆角与轻量阴影；左侧侧边栏保持不变。
- 设置项与入口继续使用原有数据、开关双向绑定和命令，仅优化字号与字重，并为现有图标增加浅灰圆形承托、细化 Switch 尺寸和低存在感分隔线。
- 完成 800×710 多轮截图叠图校正；Release 构建及 123 项测试通过，未修改设置功能、数据逻辑、交互逻辑或窗口尺寸。

## 2026-08-20 设置页状态反馈与静态卡片细化

- 设置开关开启时同步将对应左侧图标切换为橙色，关闭时继续显示灰色；复用原有 `IsChecked` 状态，未修改绑定或功能逻辑。
- 移除设置开关行和数据入口行的 Hover 背景变化，行背景改为透明，使 10 DIP 卡片圆角自然呈现。
- 移除卡片阴影并保留低存在感细边框；Release 构建及 123 项测试全部通过。

## 2026-08-20 皮肤选择弹窗设计图 2 高还原重构

- 将弹窗扩大为 560×600 DIP，标题改为“选择主题”，移除副标题、分类标签、调色板图标和关闭按钮占位，保留外部点击关闭能力。
- 主题入口按设计图统一为三行四列，使用两条轻量分隔线区分纯色、渐变和个性主题；移除“月光”可见选项，其余 12 个主题继续绑定原 `SelectThemeCommand` 与原主题键。
- 统一预览卡片尺寸、圆角、名称排版和暖阳选中态的橙色描边及圆形勾选标记；完成 560×600 截图叠图校正，Release 构建及 123 项测试通过。

## 2026-08-20 皮肤选择弹窗等比例恢复尺寸

- 将弹窗外部尺寸恢复为 420×450 DIP，并通过统一 75% 缩放保留 560×600 设计布局的全部比例。
- 标题、主题卡片、间距、分隔线、圆角和选中勾选同步缩放，未修改主题命令、数据绑定或选择逻辑；123 项测试通过。

## 2026-08-20 VIP 开通弹窗设计图 2 高还原重构

- 保持弹窗 440×550 DIP 固定尺寸不变，校准顶部 Logo、居中标题、关闭按钮、分隔线和整体留白，并移除设计稿中不存在的副标题。
- 六项权益保持原内容与顺序，改为轻量三列卡片并隐藏冗余说明；套餐数据、价格、选择命令和推荐标识逻辑均保持不变。
- 三张套餐卡片重新调整内部文字层级，选中态使用橙色描边与更浅背景；升级按钮补充白色皇冠线框并保留原升级命令及反馈文案。
- 完成 440×550 截图叠图校正和套餐/升级命令绑定验证；Release 构建及 123 项测试全部通过。

## 2026-08-20 VIP 权益标题裁剪修复

- 保持弹窗尺寸、三列卡片、12px 字号和图标列不变，仅将六张权益卡片的内部右侧留白由 9 DIP 缩减为 4 DIP。
- 为六字权益标题增加 5 DIP 可用宽度，避免运行时字体度量与 DPI 取整导致末字裁剪；440×550 渲染和 123 项测试通过。

## 2026-08-21 登录弹窗找回密码流程

- 在现有 360×360 登录弹窗内新增输入账号、验证身份、设置新密码和修改成功四步页面，不创建二级弹窗，也不改变登录与注册页面尺寸。
- 增加逐级返回、输入状态控制、6 位数字验证码、55 秒内存重发倒计时、两次密码一致校验及密码显示/隐藏交互。
- 全流程仅使用当前运行期内存假数据，未修改既有登录和注册逻辑，未接入数据库、IPC、API、服务器或后台能力；Release 构建及 139 项测试通过。

## 2026-08-21 已屏蔽内容弹窗视觉优化

- 按目标设计稿重做弹窗圆角、标题留白、浅灰圆形关闭按钮、Tab 字体层级与细橙色选中下划线。
- 优化列表大圆角容器、行高、左右留白、图标、浅橙类型标签、轻量删除入口、淡分割线和窄圆角滚动条，并使首屏完整展示五行内容。
- 仅修改 XAML 视觉样式，保留原有数据绑定、Tab 切换、取消屏蔽命令和滚动交互；Release 构建及 139 项测试通过。

## 2026-08-21 首页当前屏蔽卡片视觉优化

- 按方案 A 降低首页底部卡片视觉重量：移除阴影，保留浅灰细边框，调整为 14 DIP 圆角并增加内部左右留白。
- 标题改为 14px Medium，副标题通过现有数量绑定显示“7 个网站和应用”，并减轻预览图标、额外数量及“查看全部”箭头样式。
- 仅修改首页卡片 XAML 和文字资源，未修改数据、点击、弹窗或屏蔽逻辑；Release 构建及 139 项测试通过。

## 2026-08-22 首页已选专注目标居中

- 将专注目标按钮内容改为左右各 28 DIP 的对称三列布局，左侧保留灰色铅笔，右侧增加轻量灰色 Chevron，中间目标名称相对整个胶囊真正居中。
- 移除选中态原有的不对称右侧补偿留白，保留按钮尺寸、圆角、颜色、位置、Tooltip 和原选择命令；Release 构建及 139 项测试通过。

## 2026-08-22 专注目标选择弹窗视觉统一

- 保持 336×430 DIP 弹窗尺寸、网格布局、组件位置和全部绑定命令不变，将两级标题统一为 16px Medium，正文及标签统一为 13px，并校准时钟图标、Chevron 和浅色分割线。
- 最近目标标签补齐默认、Hover、选中和 Pressed 状态；Chevron 改为透明背景，加号改为白底细边框与极弱阴影，取消/确认按钮分别采用轻边框与纯橙实心样式。
- 移除该弹窗操作按钮的渐变和明显阴影，仅修改视觉资源与控件模板；Release 构建及 139 项测试通过。桌面截图工具受无边框 WPF 捕获接口限制，未能生成可靠截图。

## 2026-08-22 主题按钮文字基础模板

- 新增全局 `ThemedButtonTextContentTemplate` 与 `ThemedTextButtonBaseStyle`，由显式 TextBlock 直接绑定按钮的前景色、字体、字号和字重，避免全局隐式 TextBlock 样式覆盖按钮文字属性。
- 基础模板不定义任何颜色，纯文字按钮仍通过 `DynamicResource` 引用各主题资源；主题运行时替换资源后，文字绑定会跟随按钮属性更新。
- 专注目标弹窗的取消、确认及创建按钮接入该基础模板，未修改按钮尺寸、位置、背景、命令或主题切换逻辑；运行时模板验证得到取消 `#3A3A3C`、确认 `#FFFFFF`，两者均为 Medium（500）。

## 2026-08-22 统计趋势图 Y 轴坐标映射修复

### 完成
- 将 Y 轴刻度、折线数据点和面积填充统一到同一个 PlotArea 映射，修复 0 值数据点悬浮在 0h / X 轴上方的问题。
- 平滑曲线继续经过每个真实数据锚点，未修改趋势图尺寸、布局和视觉样式。

### 当前状态
- UI：完成
- 交互：完成
- 数据：Mock / 内存
- 后端：未接入
- 仅修复前端趋势图坐标映射，未改变统计业务逻辑或数据来源。

### 验证
- 验证 0h、4h、24h 分别映射到 PlotArea 底部、中间对应网格线和顶部；0 数据点与 0h 基线坐标一致。
- Release 测试 140 项全部通过。

### 下一步
- 无。
