# DEV_LOG.md

> 用途：让 AI / Codex 快速掌握项目当前状态、模块边界和最近变化。
> 本文件是简明状态摘要，不记录操作流水；具体实现与修改细节由 Git 历史追溯。

---

## 0. AI 读取与记录规则

### 开始任务前

1. 阅读「当前项目状态」「模块状态快照」「简明变更记录」。
2. 只有需要追溯实现时才查看 Git 历史和相关代码。
3. UI 任务同时读取 `UI_TYPOGRAPHY_STANDARD.md`，全局规范优先于单张参考图。
4. 必须区分 UI、内存交互、真实业务和持久化能力，不得把 Mock 数据当成正式能力。

### 什么时候记录

仅在以下情况写入：

- 完成功能闭环或修复真实 Bug。
- 做出会影响后续开发的重要交互、架构或数据决策。
- 项目阶段、模块完成度、数据状态或已知边界发生变化。
- 完成一轮可以独立描述结果的 UI 重构。

不记录单个颜色、字号、间距、圆角等微调，不记录尝试过程、重复状态或“下一步：无”。同一模块的连续调整应合并到已有记录。

### 怎么记录

- 通常只更新「简明变更记录」一个位置；模块真实状态发生变化时才同步更新状态区。
- 有效变更持续追加并永久保留，不删除旧记录。
- 每条严格使用 **两行**：第一行标题，第二行内容；记录之间不留空行。
- 内容行不超过约 **100 个汉字**，只写最终结果、关键边界及本轮实际完成的验证。
- 不罗列具体操作步骤、完整属性清单、失败尝试或可由 Git diff 直接看到的实现细节。

统一格式：

```md
- **YYYY-MM-DD｜模块｜类型**
  最终结果；未改变的关键边界。验证：仅写本轮实际完成的验证。
```

---

## 1. 当前项目状态

**最后更新：2026-08-27**

### 当前阶段

**阶段四：真实本地能力与后台联调。**

阶段三业务模块与阶段四四个本地模块已完成；强制专注现由 Service 持有权威状态并支持异常恢复，普通专注及其他页面仍按各自边界使用运行期内存 / Mock，未经确认不得扩大为服务器或云能力。

### 技术结构

- `.NET 8`、WPF、MVVM。
- 项目包含 Desktop、Agent、Service、Core、Infrastructure、Contracts 和 Tests。
- 主窗口按 **800 × 710 DIP** 进行视觉验收。

### 数据与后端边界

- 大部分页面仍使用 Mock / 当前运行期内存数据；访问控制规则已经 Desktop、Service 和 SQLite 联调，其他业务页面的数据适配继续分模块推进。
- SQLite V2、迁移、会话规则快照、存储仓储与版本化 Named Pipe 已完成；服务器 / API 不在本阶段默认范围内。
- Service / Agent 已接入访问控制执行链路；强制专注由 Service 权威计时并可在组件重启后恢复，普通专注仍不做后台恢复。
- 已存在的少量真实本地能力包括专注计时、窗口交互、exe 图标提取、favicon 获取及悬浮窗行为。
- 访问控制已具备本地代理、进程终止执行器和强制会话重启恢复；HTTPS 仍不解密。

### UI 规范

- UI 修改优先遵守 `UI_TYPOGRAPHY_STANDARD.md`。
- 视觉优化默认不得改变业务逻辑，除非需求明确授权。
- 避免重新产生页面间字号、字重和语义色不一致的问题。

---

## 2. 模块状态快照

| 模块 | UI | 前端交互 | 数据状态 | 当前说明 |
|---|---|---|---|---|
| 首页 | 已完成并持续优化 | 主要交互已完成 | 内存 / Mock | 包含专注时间、目标和当前屏蔽等 |
| 登录 / 注册 / 找回密码 | 已完成 | 前端流程已完成 | 内存假账号 | 未接真实账号服务器 |
| 个人信息 / 账号与同步 | 已完成 | 基础交互已完成 | Mock | 未接真实同步与设备管理 |
| VIP / 会员中心 | 已完成 | 前端选择流程已完成 | Mock | 未接支付与真实会员系统 |
| 专注流程 | 已完成 | 准备、计时、结束和完成流程已实现 | 强制：Service + SQLite；普通：Core 内存 | 强制会话后台权威计时并持久化完成；普通会话仍不恢复 |
| 目标 / ToDo | 已完成并持续优化 | 创建、选择、编辑、删除和完成等已实现 | 内存 | 未正式持久化 |
| 自动屏蔽规则 | 主要 UI 已完成 | 每天 / 自定义星期计算、跨日、重叠抑制及自动启动专注已实现 | Core 内存调度 | 未接持久化、后台或真实系统屏蔽 |
| 访问控制 | 主要 UI 已完成 | 规则通过 Service 启用本地代理与进程监控，Agent 处理代理设置和通知 | SQLite + Service | 强制会话按规则快照恢复；不解密 HTTPS |
| favicon / 程序图标 | 已完成 | 异步加载 | 部分真实本地能力 | 已支持 favicon 获取和 exe 图标提取 |
| 悬浮窗 | 主要状态已完成 | 吸附、折叠、Hover 展开和置顶等已实现 | 运行期状态 | 属于真实窗口行为 |
| 统计 | 主要页面已完成 | 完成记录驱动趋势、日历和目标统计 | Core 计算 + 运行期内存记录 | 无运行期记录时保留 UI Mock，未接持久化 |
| 强制模式 | 主要 UI 已完成 | VIP 权限校验、后台启动、自然结束、重连投影及防绕过已实现 | SQLite V2 + Service；Mock 会员 | Desktop / Service / Agent 重启可恢复；未接真实会员校验 |
| 跨模块状态协调 | 无新增页面 | 专注完成、规则、屏蔽预览及强制模式状态已统一同步 | 运行期内存 | 未接持久化、IPC、后台或真实系统屏蔽 |
| 本地持久化基础 | 无新增页面 | Service 已作为写入入口 | SQLite V2 | 活动强制会话及规则快照可恢复；其他业务页面仍按模块适配 |
| Desktop / Service / Agent 通信 | 无新增页面 | Desktop 获取权威会话，Agent 执行并校准当前用户代理 | Named Pipe v2 + Service + SQLite | 强制专注已后台化；普通专注保持现状 |
| 设置 | 已完成 | 开关与入口交互已实现 | 内存 | 多数系统设置未真实接入 |
| 主题 | 主要 UI 已完成 | 主题选择基础交互已实现 | 运行期状态 | 持续统一组件与字体规范 |

---

## 3. 简明变更记录

> 有效变更持续追加、不删除；每条严格两行且不留空行，同一任务只写一条。

- **2026-08-26｜开发阶段｜阶段切换**
  阶段二主要 UI 与基础交互已完成，进入阶段三业务功能；暂以内存 / Mock 实现，不接持久化、IPC、API、服务器或后台。验证：完成开发计划与模块依赖分析。
- **2026-08-26｜专注业务基础｜业务功能**
  新增 Core 专注会话引擎与内存完成记录，统一准备、正式计时、提前结束、自然完成及重复完成边界；未接持久化、IPC或后台。验证：Core 6项、专注 ViewModel 21项通过。
- **2026-08-26｜目标与任务业务｜业务功能**
  目标与任务增加稳定 ID，专注完成记录写入目标快照及本次任务 ID，并将实际时长一次性累计到目标；准备取消与无目标专注不产生目标回写。验证：相关定向测试 53 项通过。
- **2026-08-26｜访问控制业务｜业务功能**
  新增网站主域名/子域名与应用路径匹配，统一输出阻止结果并遵循启用状态和最具体规则优先；仅内存，未接系统代理或后台。验证：访问控制定向测试 7 项通过。
- **2026-08-27｜自动屏蔽调度｜业务功能**
  新增 Core 内存调度，支持每日/自定义星期、跨日和重叠规则并抑制连续重复触发；未接后台或真实屏蔽。验证：定向 32 项通过，全量 254/256 通过（2 项既有 UI 断言失败）。
- **2026-08-27｜统计计算｜业务功能**
  新增 Core 日/目标汇总与跨日切分，专注完成自动刷新趋势、日历和目标；无运行记录保留 UI Mock，未接持久化。验证：定向 87 项通过，全量 257/259 通过（2 项既有 UI 断言失败）。
- **2026-08-27｜强制模式与权限｜业务功能**
  Core 统一 VIP 启动授权，强制专注防止提前结束、返回首页和重启绕过；仍仅 Mock 会员与内存状态。验证：定向 85 项通过，全量 264/266 通过（2 项既有 UI 断言失败）。
- **2026-08-27｜跨模块状态协调｜业务功能**
  集中同步专注完成、规则、屏蔽预览和强制模式，并拒绝进行中会话重复启动；仍仅运行期内存。验证：定向 93 项通过，全量 268/270 通过（2 项既有 UI 断言失败）。
- **2026-08-27｜开发阶段｜阶段切换**
  阶段三 7 个模块完成，进入阶段四；后续按持久化、IPC、真实屏蔽和后台恢复推进，当前实现仍保持内存。验证：阶段三全量 268/270 通过（2 项既有 UI 断言失败）。
- **2026-08-27｜本地持久化基础｜真实数据**
  新增 SQLite V1、Core 存储模型、事务仓储、迁移备份及异常保护；未接 Desktop / IPC。验证：定向 8/8，全量 276/278（2 项既有 UI 失败）。
- **2026-08-27｜Desktop 与后台通信｜本地联调**
  新增版本化协议、Named Pipe 身份隔离、请求幂等、事件广播、超时与自动重连，Service 成为受支持数据的唯一 SQLite 写入口；专注计时仍未迁往后台。验证：定向 12/12，全量 288/290（2 项既有 UI 失败）。
- **2026-08-27｜真实访问控制执行｜本地联调**
  Service 通过本地代理与进程监控执行规则，Agent 安全接管并恢复用户代理、展示拦截通知；未实现 Service 重启后的完整恢复。验证：定向 21/21，全量 301/303（2 项既有 UI 失败）。
- **2026-08-27｜网站代理链｜Bug 修复**
  非黑名单流量转发至原 HTTP/SOCKS5 代理，支持 VPN 变化协调及陈旧恢复记录自愈。验证：定向 23/23，全量 312/314。
- **2026-08-27｜强制模式后台恢复｜本地联调**
  Service 权威持久化并恢复强制会话、规则和屏蔽，幂等到期且 Agent 重连校准代理；普通会话不恢复。验证：全量 328/330。
- **2026-08-26｜专注任务｜完成分组标题**
  移除“已完成 X 项”折叠行左侧勾选图标，保留整行展开收起与下方任务勾选交互。验证：220/220。
- **2026-08-26｜专注任务｜完成状态切换**
  已完成任务勾选框改为可操作命令，支持待完成与已完成集合双向切换并实时刷新数量；未修改任务数据、创建删除、拖拽与计时逻辑。验证：220/220。
- **2026-08-26｜会员中心｜UI**
  会员中心保持680×400与原有绑定，统一字体层级、卡片说明行高、间距和3×2权益布局；未修改会员逻辑与按钮行为。验证：220/220。
- **2026-08-26｜专注完成页｜任务列表滚动**
  本次完成任务超过4项时限制为四行并启用局部5px苹果风滚动条，少于等于4项保持自然高度；未改页面尺寸与完成数据逻辑。验证：220/220。
- **2026-08-26｜专注任务｜新增任务 Enter 修复**
  根级PreviewKeyDown延后提交当前编辑任务并阻止Enter继续触发新增按钮，新增命令含编辑态防重入与焦点隔离；添加任务仍只创建一条记录。验证：220/220。
- **2026-08-26｜专注任务｜任务列表结构修复**
  未完成任务与已完成折叠分组统一放入同一ScrollViewer，已完成任务展开后共享滚动区域；“+ 添加任务”移至滚动区外并始终显示。未修改任务操作与计时。验证：219/219。
- **2026-08-26｜专注任务｜完成分组交互修复**
  已完成分组整行使用显式 ViewModel 命令、透明背景与 Stretch 命中区域，修复展开/收起无响应并保持箭头同步；完成状态与任务操作不变。验证：219/219。
- **2026-08-26｜专注任务｜完成分组 UI**
  完成任务保留在内存集合并归入可展开的“已完成”分组；未完成数量、完成数量及默认收起状态同步展示，添加任务置于分组之后。未修改任务操作与计时逻辑。验证：218/218。
- **2026-08-26｜导出专注记录｜UI**
  设置入口新增400×360模态弹窗、蒙版关闭及三个互斥边界避让Popup；按全局字体规范统一三行60px共享模板、独立等距分割线及底部说明与按钮留白。仅内存选择，导出按钮不生成文件。验证：218/218。
- **2026-08-26｜专注任务｜UI / Bug 修复**
  重命名支持 MainWindow 根级外部点击提交；任务行根容器统一透明命中及Hover/拖拽事件，并支持带自动滚动、插入反馈的内存拖拽排序和5px极窄滚动条。其他逻辑与布局不变。验证：214/214。
- **2026-08-25｜专注完成页｜Bug 修复**
  「再专注一次」复用上一轮原始设定时长创建新会话并直接进入完整倒计时，自定义时长同样适用；返回首页及上一轮累计逻辑不变。验证：211/211。
- **2026-08-25｜专注结束确认｜UI**
  弹窗重构为350×250时钟布局，突出动态已专注时长并调整为左结束、右继续；命令与计时逻辑不变。验证：209/209。
- **2026-08-25｜设置强制模式｜UI**
  非VIP显示锁与专享标签且开关禁用；组合区Hover显示250×120三行说明，支持150ms延迟与边界避让。仅内存权限。验证：208/208。
- **2026-08-25｜主题切换权限｜UI**
  未登录及普通用户可 Hover 会员主题并临时预览，连续预览保留原主题，恢复或关闭弹窗自动回退；预览期间隐藏原主题选中视觉，确保仅当前预览主题高亮，底部状态栏复用现有 VIP 引导，确认 VIP 后才正式应用。锁定层受缩略图圆角裁剪，Hover 边框与预览按钮统一以缩略图容器居中且 Hover 色跟随对应主题，选中描边改为不裁剪的独立覆盖层，勾选图标独立叠加且不参与主体布局；移除弹窗整体 Viewbox 缩放并按最终尺寸换算布局，使字体以真实 DIP 遵循全局 Typography。仅前端内存状态，弹窗与主题网格视觉尺寸不变。验证：206/206。
- **2026-08-25｜统计目标投入详情｜UI**
  未登录及普通用户显示 VIP 锁定统计、趋势及记录骨架，并仅在锁图标与「VIP专享」区域 Hover 时显示 300×170 引导浮层；仅浮层按钮进入现有会员入口，VIP 用户保留完整详情交互。仅前端内存状态，右侧卡片尺寸不变。验证：199/199。
- **2026-08-25｜统计当日专注记录｜UI**
  未登录及普通用户显示 VIP 锁定骨架占位，并仅在锁图标与「VIP专享」区域 Hover 时显示 300×170 引导浮层；仅浮层按钮进入现有会员入口，VIP 用户仍显示真实日期与记录。仅前端内存状态，卡片尺寸及其他模块不变。验证：198/198。
- **2026-08-25｜统计推进轨迹｜UI**
  锁定骨架增加 300×170 VIP Hover 引导及正式会员入口，VIP 用户仍显示真实趋势图；仅前端状态，卡片尺寸及其他模块不变。验证：197/197。
- **2026-08-25｜目标选择｜Bug 修复**
  弹窗使用临时选择，确认或蒙版关闭时提交；取消目标后首页同步清空。验证：192/192。
- **2026-08-25｜专注完成页｜UI**
  统一标题、时长数字与单位、卡片信息和按钮字体层级；布局及主题不变。验证：191/191。
- **2026-08-24｜目标任务｜Bug 修复**
  无目标时任务列表为空，任务统一绑定目标并随选择切换；仅内存逻辑，布局不变。验证：190/190。
- **2026-08-24｜访问被阻止提示｜UI**
  独立 MVVM Toast 改为右下角浮出、5秒关闭及Hover暂停；仍仅Debug Mock。验证：184/184、Release构建、启动。
- **2026-08-24｜统计日历｜UI**
  右侧详情改为单行汇总与自适应记录区；完成任务使用只读勾选行，空任务不占位，数据逻辑不变。验证：178/178。
- **2026-08-24｜访问控制网站｜UI**
  网站菜单完成轻量双行样式；复用添加弹窗实现原位编辑、预填和地址校验，保留屏蔽状态；仅内存。验证：176/176。
- **2026-08-24｜首页自动屏蔽｜UI**
  自动屏蔽提示改为苹果风卡片、动态居中定位并将Hover延迟降至100ms；保留原文案与计算。验证：170/170。
- **2026-08-24｜统计总览｜Bug 修复**
  时间范围选择器拆分文字与箭头布局并改用84最小宽度，修复“近30天”裁剪且允许长标签扩展。验证：170/170。
- **2026-08-22｜开发文档｜规则**
  简明变更改为永久累积的两行记录；同一任务只写一条，同模块同日连续修改合并，不记录操作细节。
- **2026-08-22｜设置页｜UI**
  规则行改用 Switch 与中文菜单，复用弹窗完成 5 分钟时间回填及原位编辑，保留启用状态；仅内存交互。验证：测试 147/147。
- **2026-08-22｜统计目标｜UI**
  推进记录按专注时间段展示、增加只读弱灰勾选并移除冗余区标题；投入趋势与月份菜单独立交互；仅内存。验证：169/169。
- **2026-08-22｜统计目标｜Bug 修复**
  修复推进记录只读 Run.Text 默认双向绑定导致目标页崩溃；展示与数据逻辑不变。验证：运行时模板及全量测试 161/161。
- **2026-08-22｜统计日历｜UI**
  统一字体层级，弱化非零日期时长并固定占位，专注记录改为极窄滚动条；未改布局、数据和逻辑。验证：Debug 140/140。
- **2026-08-22｜统计日历｜Bug 修复**
  修复只读绑定启动崩溃及跨月补位日期误显示时长；保留日期布局、选中和数据逻辑。验证：最新 XAML 编译通过，测试 140/140。
- **2026-08-22｜统计总览｜UI**
  统一 Tab、KPI、图表坐标、汇总栏和月度目标的字体与语义色，橙色仅承担强调；未改布局和业务逻辑。验证：140 项测试通过。
- **2026-08-22｜统计趋势图｜Bug 修复**
  统一刻度、折线锚点和面积基线的 PlotArea 映射，0 值精确落在 0h 基线；未改图表结构和数据来源。验证：140 项测试通过。
- **2026-08-22｜主题按钮｜基础样式**
  新增主题文字按钮基础模板，避免隐式 TextBlock 样式覆盖按钮文字属性；未改变按钮业务交互。

---

## 4. 当前重点与已知限制

- **2026-08-28｜开机自启动｜本地联调**
  设置页通过 Service 协调 Agent 写入 HKCU\\Run，登录后静默启动 Agent，未结束强制专注时才唤醒 Desktop；普通启动项不直接指向 Desktop。验证：启动项抽象测试 4 项、定向 IPC 测试 22 项通过。

- 阶段四四个本地模块已完成验收；强制专注与真实屏蔽可后台恢复，普通专注及其他页面数据适配仍保持既有边界。
- 下一模块需重新确认范围；真实会员、服务器 API、云同步、安装自启和防卸载能力均未随本模块接入。
- Mock / 内存数据不得描述为正式业务数据或持久化数据。
- 专注目标等无边框 WPF 窗口可能无法被当前自动截图接口可靠捕获，截图失败不代表窗口未启动。
- 只有状态变化才修改模块快照；普通 UI 微调只合并到简明变更记录中的已有条目。

## 5. 阶段三模块拆分

### 5.1 专注业务基础

- 负责准备倒计时、正式计时、剩余时间、进度、普通模式提前结束和完成收口。
- 以统一的开始时间 / 结束时间计算状态，不以 UI 每秒递减作为唯一真相。
- 验收：开始、取消、暂停确认、自然结束和提前结束均能产生一致的内存状态与完成结果。

### 5.2 目标与任务业务

- 负责目标选择、任务归属、创建 / 编辑 / 删除 / 完成，以及完成任务分组。
- 专注开始时锁定当前目标和任务上下文，完成时更新任务状态与目标累计时长。
- 验收：目标切换、任务完成切换、专注完成回写和空目标边界均保持一致。

### 5.3 访问控制业务

- 负责网站域名及子域名匹配、应用规则匹配、启用状态和阻止结果。
- 将命中结果统一转换为 UI 可消费的被阻止状态，保留现有提示窗口作为展示层。
- 验收：网站 / 应用规则增删改、开关、匹配优先级和未命中行为可重复验证；不在本模块接入数据库或服务进程。

### 5.4 自动屏蔽调度

- 负责每天 / 自定义星期规则的时间计算、跨日窗口、重叠规则和当前有效规则选择。
- 规则触发后创建专注业务所需的启动请求，不直接把调度逻辑写入 View。
- 验收：跨日、边界时刻、规则冲突、重复触发抑制和应用启动时已在窗口内等场景均有测试。

### 5.5 统计计算

- 负责从内存专注记录计算日、周、月汇总、趋势、日历详情、目标投入和完成比例。
- 统一统计口径，明确跨日记录、未完成专注和超过目标后的比例展示规则。
- 验收：同一组记录在首页、日历、目标统计和总览中的累计结果一致。

### 5.6 强制模式与权限规则

- 负责普通 / VIP 能力判断、强制模式启动条件、不可提前结束状态和权限拒绝结果。
- 阶段三只实现业务层权限与状态约束，会员来源仍使用现有内存 / Mock 状态。
- 验收：非 VIP 无法启动或绕过强制模式，VIP 状态切换不会破坏普通专注流程。

### 5.7 跨模块状态协调

- 负责专注、目标任务、屏蔽、自动规则、统计、首页和悬浮窗口之间的事件与状态同步。
- 保持 View 只展示、ViewModel 只编排，主题和临时 UI 状态不得改变业务结果。
- 验收：开始 / 结束 / 取消 / 任务完成 / 规则触发后的所有相关页面状态一致，重复事件不会重复创建会话或记录。

### 5.8 阶段三明确不做

- 不接 SQLite、本地设置持久化、Service / Agent、Named Pipe、服务器 API、账号云同步和真实会员校验。
- 不要求系统重启恢复、主窗口关闭后后台继续运行或真实系统级网站 / 应用拦截；这些属于阶段四及后续联调范围。

### 5.9 模块实施顺序与门槛

1. 先完成专注业务基础，并以单元测试固定状态转换。
2. 再接入目标任务回写，确认专注完成结果稳定后扩展访问控制。
3. 自动屏蔽依赖专注启动入口，统计依赖统一记录模型，强制模式依赖权限与专注状态。
4. 最后完成跨模块协调；每个模块验收通过后再进入下一个模块。

阶段三完成标准：现有 UI 可以驱动真实的内存业务流程，核心规则和边界有可重复测试，但所有数据在进程结束后仍可丢失，且不依赖数据库、IPC、API、服务器或后台服务。

---

## 6. 阶段四模块拆分

### 6.0 总体依赖与边界

- 固定顺序：本地持久化基础 → Desktop / Service / Agent 通信 → 真实访问控制执行 → 强制模式后台恢复。
- Service 最终是核心业务状态与本地数据库的唯一写入口；Desktop 不直接引用 Infrastructure 或打开 SQLite。
- 第一模块先验收存储层关闭后重新打开的数据恢复，Desktop 端到端重启恢复在 IPC 接通后验收。
- 阶段三的 Core 规则继续复用；阶段四只替换数据来源和执行宿主，不把 UI、主题或 Mock 状态变成业务真相。

### 6.1 本地持久化基础（已完成）

- 统一当前分散在专注目标、统计目标和页面 ViewModel 中的数据，定义可重新装载的 Core 模型与存储接口；SQLite 实现放在 Infrastructure。
- 持久化专注会话与完成记录、目标与任务、网站与应用规则、自动规则，以及业务所需设置；稳定保存 ID、排序、启用 / 归档状态和历史快照。
- 活动会话至少保存状态、模式、配置时长、准备 / 正式开始时间、计划结束时间、目标快照、任务快照、屏蔽状态和自动规则来源，为后续后台恢复提供数据。
- 设置范围包括现有业务开关、主题、常用专注时长及顺序、当前时长和月度专注目标；统计汇总仍从专注记录计算，不另存一份权威累计值。
- 数据库按 Windows 用户隔离，路径由可注入的数据目录提供器决定；不得直接使用 Windows Service 账户的 LocalAppData 代替当前用户数据目录。
- 使用 `PRAGMA user_version` 顺序迁移、外键、索引和事务；升级前创建一致性备份，升级后执行完整性检查。
- 关键写入保持原子性；会话完成、完成任务和历史记录必须一起成功或一起回滚，并通过稳定会话 ID 保证幂等。
- 数据库锁定采用有限重试；迁移、损坏、权限或磁盘错误必须明确返回，不得静默删除、覆盖或重建用户数据库。
- 时间以 UTC / `DateTimeOffset` 持久化，显示时再转换为本地时间；Mock 目标、Mock 记录、假账号和临时 UI 状态不得写入数据库。
- 验收：新库可初始化到当前版本；所有范围内数据关闭连接后重新打开仍完整一致；活动会话最多一条；迁移失败保留旧版本和旧数据；异常场景有可重复集成测试。
- 验收：Desktop 仍不直接依赖 Infrastructure；阶段三全量测试不新增失败，新增持久化测试全部通过。
- 完成结果：存储层定向测试 8/8；解决方案构建 0 警告、0 错误；全量 276/278，仅保留两项既有 UI 展示断言失败。

### 6.2 Desktop 与后台通信（通信层已完成）

- 在 Contracts 定义可版本化、可序列化的 Commands、Queries、Events、DTO 和 ErrorResult，协议不包含 WPF 类型或业务实现。
- Service 持有权威专注、规则和持久化状态；Desktop 只发送请求、订阅事件并在连接或重连后获取完整状态快照。
- Agent 只处理托盘、通知、唤起 Desktop 和必须位于用户会话内的 Windows 操作，不保存核心业务状态或决定规则。
- Named Pipe 需要身份校验、协议版本、请求关联 ID、超时、取消、健康检查、断线重连和重复命令幂等处理。
- 后台不可用、超时、协议不兼容和业务拒绝必须分别反馈；不得回退为 Desktop 自行计时、写库或执行屏蔽。
- 验收：正常请求、并发请求、重复请求、断线、Service 重启、协议错误及完整状态重取均有自动化测试，Desktop 与 Service 状态最终一致。
- 完成边界：Contracts、Named Pipe、Service 数据命令、Desktop 状态客户端及 Agent 健康 / 状态连接已落地；Desktop 根状态可观察后台连接与错误，业务页面不直接打开 SQLite。
- 后续边界：网站 / 应用规则的真实执行在 6.3 接入；专注会话的后台权威计时、完成写入与恢复在 6.4 接入，本模块不把现有内存计时描述为后台能力。
- 完成结果：通信定向测试 12/12；解决方案构建 0 警告、0 错误；全量 288/290，仅保留两项既有 UI 展示断言失败。

### 6.3 真实访问控制执行（已完成）

- 复用 Core 的域名 / 子域名和应用路径匹配，把命中结果接入 Infrastructure 的网站代理与进程检测执行器。
- 网站屏蔽仅按域名工作，不解密 HTTPS、不按 URL 路径匹配；既有 HTTP/SOCKS5 代理作为上游，PAC 与自动代理仍拒绝接管。
- 应用屏蔽覆盖新启动和已运行程序，按规范化完整路径命中并终止；不引入驱动级拦截。
- Service 负责规则快照和执行状态，Agent 展示用户会话通知；执行失败必须记录并上报真实状态，不得仅因规则命中就报告已拦截。
- 验收：HTTPS CONNECT 和 HTTP 转发均按域名规则验证，应用规则覆盖已有与新启动进程；规则热更新、代理保存 / 恢复 / 冲突、Agent 确认和 IPC 联调均有自动化测试。
- 完成边界：规则由 Desktop 写入 Service，Service 按用户 SID 启动执行器，Agent 仅操作当前用户代理和展示通知；尚未做端到端手工浏览器 / 真实应用验证。
- 后续边界：Service 重启后重新恢复访问控制，以及专注结束后后台重新接管规则，属于 6.4，不得描述为本模块已完成能力。
- 完成结果：访问控制定向测试 23/23；全量 312/314，仅保留两项既有 UI 展示断言失败；解决方案构建 0 错误、0 警告。

### 6.4 强制模式后台恢复（已完成）

- 将专注真相从 Desktop 的运行期 Stopwatch 调整为 Service 持有的开始时间、计划结束时间和当前时间计算结果。
- 开始成功前先事务持久化活动会话；强制专注期间由 Service 维持计时和屏蔽，Desktop 关闭不影响业务状态。
- Service 启动时检查未结束会话：只恢复符合条件的强制专注，重新应用屏蔽，并通过 Agent 唤起或通知 Desktop 获取完整状态。
- 自然到期必须只生成一次完成记录并清理活动状态；重复启动、重复恢复、时钟跳变和异常退出不得产生重复记录或错误延长时长。
- 普通模式是否恢复不在本模块擅自扩大；未形成明确产品规则前，不得按强制模式逻辑恢复普通会话。
- 验收：关闭 Desktop、重启 Service、模拟系统重启和恢复过程中再次异常退出后，强制专注仍按原计划结束且记录唯一；普通专注不被错误恢复。
- 完成边界：SQLite V2 保存会话规则快照，Service 权威调度并幂等收尾，Desktop 重连只投影后台会话，Agent 重连校准代理；普通专注仍不恢复。
- 完成结果：相关定向测试 67/67；解决方案独立输出构建 0 警告、0 错误；全量 328/330，仅保留两项既有 UI 展示断言失败。

### 6.5 阶段四明确不做

- 不接服务器 API、云同步、同步元数据、真实账号、真实会员订阅、支付或多设备账户管理；这些能力需另行拆分和授权。
- 不进行 HTTPS 内容解密、按 URL 路径屏蔽、驱动级应用拦截或复杂代理兼容扩展。
- 不把阶段三 Mock 数据迁移为用户数据，不持久化 Hover、Popup、Toast、展开状态等临时 UI 状态。
- 不在本阶段顺带实现导出记录、删除全部数据或其他未进入当前模块的设置入口能力。

### 6.6 图标磁盘缓存（已完成）

- Desktop 图标读取统一采用内存缓存、磁盘 PNG 缓存和原始获取的三级流程；网站按规范化域名缓存，应用按规范化 EXE 路径缓存。
- PNG 文件保存到 `%LocalAppData%\\FocusApp\\Cache\\Icons\\Websites` 或 `Applications`，文件名使用 CacheKey 的 SHA256；SQLite 仅保存 `IconCache` 元数据。
- 网站缓存有效期 30 天，过期先返回旧图并后台刷新；网络失败保留旧图并进入 24 小时重试冷却。应用缓存比较 EXE 的最后写入时间和文件大小，文件变化后重新提取。
- 相同 CacheKey 的并发请求共享同一个获取任务；获取失败会按网站或 EXE 来源状态抑制重复请求，不影响规则保存和 UI 加载。
- 验收：图标缓存定向测试 4/4；全量测试 332/334，仅保留两项既有 UI 展示断言失败。
