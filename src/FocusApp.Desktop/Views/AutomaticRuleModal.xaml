<UserControl x:Class="FocusApp.Desktop.Views.AutomaticRuleModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:FocusApp.Desktop.Views"
             Width="270"
             Background="Transparent"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Style>
        <Style TargetType="{x:Type UserControl}">
            <Setter Property="Height" Value="360" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCustom}" Value="True">
                    <Setter Property="Height" Value="440" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="RuleModeButtonStyle" TargetType="{x:Type RadioButton}">
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="{DynamicResource SurfacePrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border x:Name="ModeBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ModeBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ModeBorder" Property="Background" Value="{DynamicResource ControlPressedBackground}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ModeBorder" Property="Background" Value="{DynamicResource AccentPrimary}" />
                                <Setter TargetName="ModeBorder" Property="BorderBrush" Value="{DynamicResource AccentPrimary}" />
                                <Setter Property="Foreground" Value="{DynamicResource WhiteText}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WeekdayButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="{DynamicResource SurfacePrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="DayBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource ControlPressedBackground}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource AccentPrimary}" />
                                <Setter TargetName="DayBorder" Property="BorderBrush" Value="{DynamicResource AccentPrimary}" />
                                <Setter Property="Foreground" Value="{DynamicResource WhiteText}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RuleTimeTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Width" Value="92" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="{DynamicResource SurfacePrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="{DynamicResource FontFamilyNumeric}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource AccentHover}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource AccentPrimary}" />
                                <Setter TargetName="InputBorder" Property="BorderThickness" Value="1.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RuleSecondaryButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="72" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{DynamicResource SurfacePrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBackground}" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlPressedBackground}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RulePrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource RuleSecondaryButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentPrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimary}" />
            <Setter Property="Foreground" Value="{DynamicResource WhiteText}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentHover}" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentPressed}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource SurfacePrimary}"
            BorderBrush="{DynamicResource OverlayBorder}"
            BorderThickness="1"
            CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" Direction="270" Opacity="0.16" ShadowDepth="5" />
        </Border.Effect>
        <Canvas>
            <TextBlock Canvas.Left="18" Canvas.Top="16" FontSize="18" FontWeight="SemiBold" Text="{DynamicResource AutomaticRuleNewTitle}" />
            <Button Canvas.Left="228" Canvas.Top="12" Width="28" Height="28" AutomationProperties.Name="{DynamicResource AutomaticRuleClose}" Command="{Binding CloseCommand}" Style="{StaticResource AuthCloseButtonStyle}">
                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="13" Text="&#xE8BB;" />
            </Button>
            <Border Canvas.Left="18" Canvas.Top="54" Width="234" Height="1" Background="{DynamicResource BorderPrimary}" />

            <TextBlock Canvas.Left="18" Canvas.Top="70" FontSize="14" FontWeight="SemiBold" Text="{DynamicResource AutomaticRuleRepeatTitle}" />
            <Grid Canvas.Left="18" Canvas.Top="94" Width="234" Height="30">
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0" Content="{DynamicResource AutomaticRuleDaily}" Command="{Binding SelectDailyCommand}" IsChecked="{Binding IsDaily, Mode=OneWay}" Style="{StaticResource RuleModeButtonStyle}" />
                <RadioButton Grid.Column="1" Content="{DynamicResource AutomaticRuleCustom}" Command="{Binding SelectCustomCommand}" IsChecked="{Binding IsCustom, Mode=OneWay}" Style="{StaticResource RuleModeButtonStyle}" />
            </Grid>

            <StackPanel Canvas.Left="18" Canvas.Top="132" Visibility="{Binding IsCustom, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{DynamicResource AutomaticRuleChooseWeekdays}" />
                <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Weekdays}">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle><Style TargetType="{x:Type ContentPresenter}"><Setter Property="Margin" Value="0,0,5,0" /></Style></ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton AutomationProperties.Name="{Binding DisplayName}" Content="{Binding ShortName}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Style="{StaticResource WeekdayButtonStyle}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Margin="0,10,0,0" FontSize="12" Foreground="{DynamicResource TextSecondary}">
                    <Run Text="{DynamicResource AutomaticRuleSelectedPrefix}" /><Run Text="{Binding SelectedDaysText, Mode=OneWay}" />
                </TextBlock>
            </StackPanel>
            <Border Canvas.Left="18" Canvas.Top="218" Width="234" Height="1" Background="{DynamicResource BorderPrimary}" Visibility="{Binding IsCustom, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <TextBlock Canvas.Left="18" FontSize="14" FontWeight="SemiBold" Text="{DynamicResource AutomaticRuleTimeTitle}">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Canvas.Top" Value="145" />
                        <Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="224" /></DataTrigger></Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBox Canvas.Left="18" AutomationProperties.Name="{DynamicResource AutomaticRuleStartTime}" Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.Style>
                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource RuleTimeTextBoxStyle}">
                        <Setter Property="Canvas.Top" Value="178" />
                        <Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="258" /></DataTrigger></Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <TextBlock Canvas.Left="130" FontSize="18" Text="–">
                <TextBlock.Style><Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}"><Setter Property="Canvas.Top" Value="185" /><Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="265" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
            </TextBlock>
            <TextBox Canvas.Left="160" AutomationProperties.Name="{DynamicResource AutomaticRuleEndTime}" Text="{Binding EndTimeText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.Style>
                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource RuleTimeTextBoxStyle}">
                        <Setter Property="Canvas.Top" Value="178" />
                        <Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="258" /></DataTrigger></Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <views:TimeRangeSlider Canvas.Left="18" Width="234" StartValue="{Binding StartValue, Mode=TwoWay}" EndValue="{Binding EndValue, Mode=TwoWay}">
                <views:TimeRangeSlider.Style>
                    <Style TargetType="{x:Type views:TimeRangeSlider}">
                        <Setter Property="Canvas.Top" Value="238" />
                        <Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="318" /></DataTrigger></Style.Triggers>
                    </Style>
                </views:TimeRangeSlider.Style>
            </views:TimeRangeSlider>

            <Border Canvas.Left="18" Width="234" Height="1" Background="{DynamicResource BorderPrimary}">
                <Border.Style><Style TargetType="{x:Type Border}"><Setter Property="Canvas.Top" Value="296" /><Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="376" /></DataTrigger></Style.Triggers></Style></Border.Style>
            </Border>
            <Button Canvas.Left="96" Content="{DynamicResource AutomaticRuleCancel}" Command="{Binding CloseCommand}">
                <Button.Style><Style TargetType="{x:Type Button}" BasedOn="{StaticResource RuleSecondaryButtonStyle}"><Setter Property="Canvas.Top" Value="310" /><Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="390" /></DataTrigger></Style.Triggers></Style></Button.Style>
            </Button>
            <Button Canvas.Left="180" Content="{DynamicResource AutomaticRuleAdd}" Command="{Binding ConfirmCommand}">
                <Button.Style><Style TargetType="{x:Type Button}" BasedOn="{StaticResource RulePrimaryButtonStyle}"><Setter Property="Canvas.Top" Value="310" /><Style.Triggers><DataTrigger Binding="{Binding IsCustom}" Value="True"><Setter Property="Canvas.Top" Value="390" /></DataTrigger></Style.Triggers></Style></Button.Style>
            </Button>
        </Canvas>
    </Border>
</UserControl>
