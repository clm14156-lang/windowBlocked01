<UserControl x:Class="FocusApp.Desktop.Views.CircularProgressRing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsHitTestVisible="False">
    <Grid>
        <Ellipse x:Name="TrackEllipse"
                 Stroke="{Binding TrackBrush, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                 StrokeThickness="{Binding RingThickness, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
        <Ellipse x:Name="FullProgressEllipse"
                 Stroke="{Binding ProgressBrush, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                 StrokeThickness="{Binding RingThickness, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                 Visibility="Collapsed" />
        <Path x:Name="ProgressPath"
              Stroke="{Binding ProgressBrush, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
              StrokeEndLineCap="Round"
              StrokeStartLineCap="Round"
              StrokeThickness="{Binding RingThickness, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
        <Ellipse x:Name="ProgressEndPoint"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Top"
                 Fill="{Binding ProgressBrush, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                 IsHitTestVisible="False"
                 Visibility="Collapsed" />
    </Grid>
</UserControl>
