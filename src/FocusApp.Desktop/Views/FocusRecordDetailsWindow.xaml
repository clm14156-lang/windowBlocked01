<Window x:Class="FocusApp.Desktop.Views.FocusRecordDetailsWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Width="340" SizeToContent="Height" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="#01FFFFFF" ShowInTaskbar="False" WindowStartupLocation="Manual"
 FontFamily="Segoe UI Variable, Microsoft YaHei UI, Segoe UI" FontSize="13" Foreground="#1D1D1F" PreviewKeyDown="Window_KeyDown">
 <Window.Resources>
  <BooleanToVisibilityConverter x:Key="BoolVisibility"/>
  <Style x:Key="PopoverActionButtonStyle" TargetType="Button">
   <Setter Property="Width" Value="28"/><Setter Property="Height" Value="28"/>
   <Setter Property="Cursor" Value="Hand"/><Setter Property="Foreground" Value="#85858F"/>
   <Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
    <Border x:Name="ActionBackground" Background="{TemplateBinding Background}" CornerRadius="8">
     <TextBlock Text="{TemplateBinding Content}" FontSize="{TemplateBinding FontSize}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
    <ControlTemplate.Triggers>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ActionBackground" Property="Background" Value="#F7F7F8"/></Trigger>
     <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ActionBackground" Property="Background" Value="#F7F7F8"/></Trigger>
     <Trigger Property="IsPressed" Value="True"><Setter TargetName="ActionBackground" Property="Background" Value="#EFEFF1"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
 </Window.Resources>
 <Border x:Name="PopoverBody" Margin="6" Padding="18" Background="White" BorderBrush="#ECECEF" BorderThickness="1" CornerRadius="10">
  <Border.RenderTransform><TranslateTransform x:Name="PopoverOffset"/></Border.RenderTransform>
  <Border.Triggers>
   <EventTrigger RoutedEvent="Loaded"><BeginStoryboard><Storyboard>
    <DoubleAnimation Storyboard.TargetName="PopoverBody" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18"/>
    <DoubleAnimation Storyboard.TargetName="PopoverOffset" Storyboard.TargetProperty="Y" From="3" To="0" Duration="0:0:0.18"/>
   </Storyboard></BeginStoryboard></EventTrigger>
  </Border.Triggers>
  <StackPanel>
   <Grid Margin="0,6,0,6">
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/></Grid.ColumnDefinitions>
    <TextBlock Text="{Binding TimeRangeDisplay}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="NoWrap"/>
    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
     <Button x:Name="MoreButton" Content="⋯" FontSize="20" Style="{StaticResource PopoverActionButtonStyle}" AutomationProperties.Name="更多操作" Click="More_Click">
      <Button.ContextMenu>
       <ContextMenu x:Name="MoreMenu" Placement="Bottom" VerticalOffset="4" HasDropShadow="False" Closed="MoreMenu_Closed">
        <ContextMenu.Template><ControlTemplate TargetType="ContextMenu">
         <Border Background="White" BorderBrush="#ECECEF" BorderThickness="1" CornerRadius="8" Padding="4"><ItemsPresenter/></Border>
        </ControlTemplate></ContextMenu.Template>
        <MenuItem x:Name="DeleteButton" Header="删除记录" Foreground="#FF3B30" FontSize="13" Click="Delete_Click">
         <MenuItem.Template><ControlTemplate TargetType="MenuItem">
          <Border x:Name="DeleteMenuBackground" MinWidth="112" Padding="12,8" CornerRadius="5" Background="Transparent">
           <ContentPresenter ContentSource="Header" TextElement.Foreground="{TemplateBinding Foreground}"/>
          </Border>
          <ControlTemplate.Triggers>
           <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="DeleteMenuBackground" Property="Background" Value="#FFF3F2"/></Trigger>
           <Trigger Property="IsEnabled" Value="False"><Setter TargetName="DeleteMenuBackground" Property="Opacity" Value="0.45"/></Trigger>
          </ControlTemplate.Triggers>
         </ControlTemplate></MenuItem.Template>
        </MenuItem>
       </ContextMenu>
      </Button.ContextMenu>
     </Button>
     <Button Content="×" Margin="4,0,0,0" FontSize="20" Style="{StaticResource PopoverActionButtonStyle}" AutomationProperties.Name="关闭" Click="Close_Click"/>
    </StackPanel>
   </Grid>
   <TextBlock Margin="0,0,0,18" Foreground="#98989F" FontSize="12" TextWrapping="NoWrap">
    <Run Text="{Binding CalendarDateDisplay, Mode=OneWay}"/><Run Text=" · "/><Run Text="{Binding CalendarDurationDisplay, Mode=OneWay}"/>
   </TextBlock>
   <Border Background="#F7F7F9" CornerRadius="8" Padding="12" MinHeight="48" Visibility="{Binding HasGoal, Converter={StaticResource BoolVisibility}}">
    <Grid>
     <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/></Grid.ColumnDefinitions>
     <Image RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding IconSource}" Width="22" Height="22"/>
     <TextBlock Grid.Column="1" Margin="10,0" Text="{Binding GoalName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
     <TextBlock Grid.Column="2" Text="›" FontSize="18" Foreground="#98989F" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
   </Border>
   <StackPanel Visibility="{Binding ShowDetailTasks, Converter={StaticResource BoolVisibility}}">
    <Border Height="1" Background="#F0F0F2" Margin="0,16"/>
    <Grid Margin="0,0,0,10">
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <TextBlock Text="完成任务" FontSize="15" FontWeight="SemiBold"/>
     <TextBlock Grid.Column="1" Text="{Binding CompletedTaskCount}" Foreground="#98989F" FontSize="13" VerticalAlignment="Center"/>
    </Grid>
    <ScrollViewer MaxHeight="240" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
     <ItemsControl ItemsSource="{Binding CompletedTaskNames}">
      <ItemsControl.ItemTemplate><DataTemplate>
       <Grid MinHeight="32" Margin="0,2">
        <Grid.ColumnDefinitions><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Image RenderOptions.BitmapScalingMode="HighQuality" Width="16" Height="16" VerticalAlignment="Center" Source="/FocusApp.Desktop;component/Assets/Icons/Common/green_gouxuan.png"/>
        <TextBlock Grid.Column="1" Margin="10,5,0,5" VerticalAlignment="Center" Text="{Binding}" TextWrapping="Wrap" LineHeight="20"/>
       </Grid>
      </DataTemplate></ItemsControl.ItemTemplate>
     </ItemsControl>
    </ScrollViewer>
    <Border Height="1" Background="#F0F0F2" Margin="0,16,0,4"/>
   </StackPanel>
   <TextBlock x:Name="DeleteError" Visibility="Collapsed" Text="删除失败，请稍后重试" Foreground="#FF3B30" FontSize="12" Margin="0,10,0,0"/>
  </StackPanel>
 </Border>
</Window>
